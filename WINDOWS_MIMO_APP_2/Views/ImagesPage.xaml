<base:ViewBase
    x:Class="WINDOWS_MIMO_APP_2.Views.ImagesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WINDOWS_MIMO_APP_2.Views"
    xmlns:base="using:WINDOWS_MIMO_APP2.Views.Base"
    DataContext="{Binding ImagesVM, Source={StaticResource Locator}}"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <RelativePanel Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel x:Name="LayoutRoot" Background="Transparent">
            <TextBlock x:Name="texto" TextWrapping="Wrap" TextAlignment="Center" Text="{Binding Title}" FontSize="32"/>
            <FlipView ItemsSource="{Binding ImageList}">
                <FlipView.ItemTemplate>
                    <DataTemplate>
                        <RelativePanel Height="auto">
                            <Image RelativePanel.AlignHorizontalCenterWithPanel="True" RelativePanel.AlignTopWithPanel="True" x:Name="Image" Source="{Binding}"/>
                        </RelativePanel>
                       
                    </DataTemplate>
                </FlipView.ItemTemplate>
            </FlipView>
            
        </StackPanel>
    </RelativePanel>
</base:ViewBase>
