<base:ViewBase
    x:Class="WINDOWS_MIMO_APP_2.Views.RecipePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WINDOWS_MIMO_APP_2.Views"
    xmlns:base="using:WINDOWS_MIMO_APP2.Views.Base"
    DataContext="{Binding RecipeVM, Source={StaticResource Locator}}"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <RelativePanel Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <TextBlock x:Name="texto"  TextWrapping="Wrap" TextAlignment="Center" Text="{Binding Message}" RelativePanel.AlignHorizontalCenterWithPanel="True" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="32"  />
        <Button RelativePanel.Below="texto" x:Name="button" Content="Task List" Command="{Binding GoToTaskListPageCommand}" HorizontalAlignment="Center" VerticalAlignment="Center" />
        <Button RelativePanel.Below="button" x:Name="favorite" Content="Favorite" Command="{Binding AddToFavoritesCommand}" HorizontalAlignment="Center" VerticalAlignment="Center" />
        <TextBlock RelativePanel.Below="favorite"  Text="{Binding Title,Mode=TwoWay}" />
    </RelativePanel>
</base:ViewBase>
