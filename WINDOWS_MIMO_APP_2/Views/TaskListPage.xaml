<base:ViewBase
    x:Class="WINDOWS_MIMO_APP_2.Views.TaskListPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WINDOWS_MIMO_APP_2.Views"
    xmlns:base="using:WINDOWS_MIMO_APP2.Views.Base"
    DataContext="{Binding TaskListVM, Source={StaticResource Locator}}"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <RelativePanel Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <TextBlock x:Name="texto" RelativePanel.AlignHorizontalCenterWithPanel="True" Text="{Binding Message}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="32"/>
        <TextBlock Text="PAGINA Tasks" RelativePanel.Below="TaskDescription"/>
        <Button x:Name="TaskDescription" RelativePanel.Below="texto" Content="Tarea 1" Command="{Binding GoToTaskPageCommand}" HorizontalAlignment="Left" VerticalAlignment="Top"/>
    </RelativePanel>
</base:ViewBase>
