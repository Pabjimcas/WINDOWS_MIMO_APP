<base:ViewBase
    x:Class="WINDOWS_MIMO_APP_2.Views.TaskListPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WINDOWS_MIMO_APP_2.Views"
    xmlns:base="using:WINDOWS_MIMO_APP2.Views.Base"
    DataContext="{Binding TaskListVM, Source={StaticResource Locator}}"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <TextBlock x:Name="texto" Text="{Binding Message}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="32"/>
        <TextBlock Text="PAGINA Tasks" Margin="10,206,-10,378"/>
        <Button x:Name="button" Content="Atras&#xD;&#xA;" Command="{Binding }" HorizontalAlignment="Left" Margin="65,62,0,0" VerticalAlignment="Top"/>
    </Grid>
</base:ViewBase>
